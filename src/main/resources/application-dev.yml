server:
  port: 8081
# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/vueblog?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
  redis:
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms
    timeout: 5000
    port: 6379
    database: 0
    host: 127.0.0.1
  elasticsearch:
    uris: 101.33.227.28:9200
    username:
    password:

shiro-redis:
  enable: true
  redis-manager:
    host: 127.0.0.1:6379


